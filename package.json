{
  "name": "unistory",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean:output": "rimraf dist",
    "dev": "npm run clean:output && parcel src/pages/index.pug",
    "build-prod": "npm run clean:output && parcel build src/pages/index.pug --no-content-hash",
    "public-gh": "npm run build-prod && gh-pages -d dist",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.0.1",
    "smooth-scroll-into-view-if-needed": "^1.1.32",
    "swiper": "^6.8.4",
    "typed.js": "^2.0.12",
    "vanilla-hamburger": "^0.2.3",
    "wc-typed-js": "^1.0.1",
    "wowjs": "^1.1.3"
  },
  "devDependencies": {
    "@parcel/transformer-pug": "^2.0.1",
    "@parcel/transformer-sass": "^2.0.1",
    "@prettier/plugin-pug": "^1.19.0",
    "gh-pages": "^3.2.3",
    "parcel-plugin-clean-dist": "^0.0.6",
    "prettier": "^2.5.1",
    "prettier-plugin-style-order": "^0.2.2",
    "rimraf": "^3.0.2"
  }
}
